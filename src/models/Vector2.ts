/**
 * A Vector2 is a (x, y) location within a PIXI container.
 */
export interface Vector2 {
    /** The x location of the vector. */
    x: number;
    /** The y location of the vector. */
    y: number;
}